package OCP;

/***
 * 
 *
 *
 *	Classe que calcula o interesse de acordo com o tipo
 *
 *	Todas as vezes que eu aumentar um tipo no sistema, terei que modificar essa classe
 *
 *
 */
public class OCPProblema {

	private String tipo;
	
	public void calculaInteresse() {
		if(tipo.equalsIgnoreCase("movie")) {
			//calcula interesse
		} else if(tipo.equalsIgnoreCase("TVShow")) {
			//calcula interesse
		}
	}
}
